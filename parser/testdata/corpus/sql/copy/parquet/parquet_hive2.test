# source: test/sql/copy/parquet/parquet_hive2.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

create or replace table orders(m int,v int,j int);
--
ok
==
insert into orders select i%12+1,i,j from range(360)t(i),range(1000)s(j);
--
ok
==
copy (select 2000+(v//12)y,m,v,j from orders) TO '{TEST_DIR}/orders_m' (FORMAT PARQUET, PARTITION_BY (m));
--
ok
==
SELECT AVG(y), AVG(m), AVG(v), AVG(j) FROM '{TEST_DIR}/orders_m/**/*.parquet'
--
ok
==
copy (select 2000+(v//12)y,m,v,j from orders) TO '{TEST_DIR}/orders_y' (FORMAT PARQUET, PARTITION_BY (y));
--
ok
==
SELECT AVG(y), AVG(m), AVG(v), AVG(j) FROM '{TEST_DIR}/orders_y/**/*.parquet'
--
ok
==
copy (select 2000+(v//12)y,m,v,j from orders) TO '{TEST_DIR}/orders_ym' (FORMAT PARQUET,PARTITION_BY (y,m));
--
ok
==
SELECT AVG(y), AVG(m), AVG(v), AVG(j) FROM '{TEST_DIR}/orders_ym/**/*.parquet'
--
ok
==
copy (select 2000+(v//12)y,m,v,j from orders order by random()) TO '{TEST_DIR}/orders_ym_rand' (FORMAT PARQUET,PARTITION_BY (y,m));
--
ok
==
SELECT AVG(y), AVG(m), AVG(v), AVG(j) FROM '{TEST_DIR}/orders_ym_rand/**/*.parquet'
--
ok
==
