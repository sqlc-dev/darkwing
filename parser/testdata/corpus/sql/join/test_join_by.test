# source: test/sql/join/test_join_by.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE t1(a INTEGER);
--
ok
==
INSERT INTO t1 VALUES (1), (2), (3), (NULL);
--
ok
==
CREATE TABLE t2(b INTEGER);
--
ok
==
INSERT INTO t2 VALUES (2), (3), (NULL);
--
ok
==
SELECT a, __mark_join_marker FROM t1 JOIN BY (TYPE mark) t2 ON (a = b) ORDER BY a NULLS LAST;
--
ok
==
SELECT a, a IN (SELECT b FROM t2) FROM t1 ORDER BY a NULLS LAST;
--
ok
==
SELECT a FROM t1 JOIN BY (TYPE semi) t2 ON (a = b) ORDER BY a NULLS LAST;
--
ok
==
SELECT a FROM t1 JOIN BY (TYPE anti) t2 ON (a = b) ORDER BY a NULLS LAST;
--
ok
==
SELECT a, b FROM t1 JOIN BY (TYPE single) t2 ON (a = b) ORDER BY a NULLS LAST;
--
ok
==
SELECT count(*) FROM t1 JOIN BY (TYPE inner) t2 ON (a = b);
--
ok
==
SELECT count(*) FROM t1 JOIN BY (TYPE left) t2 ON (a = b);
--
ok
==
SELECT count(*) FROM t1 JOIN BY (TYPE full) t2 ON (a = b);
--
ok
==
SELECT a FROM t1 JOIN BY (TYPE SeMi) t2 ON (a = b) ORDER BY a NULLS LAST;
--
ok
==
SELECT a, __mark_join_marker FROM t1 JOIN BY (TYPE mark_join) t2 ON (a = b) ORDER BY a NULLS LAST;
--
ok
==
SELECT count(*) FROM t1 JOIN BY (TYPE semi) (SELECT b AS a FROM t2) USING (a);
--
ok
==
SELECT * FROM t1 JOIN BY (TYPE bogus) t2 ON (a = b);
--
ok
==
SELECT * FROM t1 JOIN BY (TYPE invalid) t2 ON (a = b);
--
error: Parser Error: "invalid" is not a valid join type for JOIN BY
==
CREATE TABLE mark(mark INTEGER, single INTEGER);
--
ok
==
INSERT INTO mark VALUES (1, 2);
--
ok
==
SELECT mark, single FROM mark;
--
ok
==
