# source: test/sql/copy/parquet/shredded_variant_stats.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

COPY (SELECT * from (VALUES
        (21::VARIANT),
        (42::VARIANT),
    ) t(col)
) to '{TEST_DIR}/fully_shredded_primitive.parquet'
--
ok
==
select s.shredding_state, s.fully_shredded.stats.min, s.fully_shredded.stats.max, s.fully_shredded.stats.has_null, s.fully_shredded.stats.has_no_null FROM (select stats(col) s from '{TEST_DIR}/fully_shredded_primitive.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (NULL::VARIANT),
        (21::VARIANT),
        (NULL::VARIANT),
        (42::VARIANT),
    ) t(col)
) to '{TEST_DIR}/fully_shredded_primitive_with_nulls.parquet'
--
ok
==
select s.shredding_state, s.fully_shredded.stats.min, s.fully_shredded.stats.max, s.fully_shredded.stats.has_null, s.fully_shredded.stats.has_no_null from (select stats(col) s from '{TEST_DIR}/fully_shredded_primitive_with_nulls.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (['test']::VARIANT),
        (21::VARIANT),
        (42::VARIANT),
    ) t(col)
) to '{TEST_DIR}/not_fully_shredded_primitive.parquet'
--
ok
==
select s.shredding_state, s.partially_shredded.stats.min, s.partially_shredded.stats.max, s.partially_shredded.stats.has_null, s.partially_shredded.stats.has_no_null from (select stats(col) s from '{TEST_DIR}/not_fully_shredded_primitive.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (['test']::VARIANT),
        ([21]::VARIANT),
    ) t(col)
) to '{TEST_DIR}/fully_shredded_list.parquet'
--
ok
==
select s.shredding_state, s.has_null, s.has_no_null from (select stats(col) s from '{TEST_DIR}/fully_shredded_list.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (NULL::VARIANT),
        (NULL::VARIANT),
        (['test']::VARIANT),
        ([21]::VARIANT),
        (NULL::VARIANT)
    ) t(col)
) to '{TEST_DIR}/fully_shredded_list_with_nulls.parquet'
--
ok
==
select s.shredding_state, s.has_null, s.has_no_null from (select stats(col) s from '{TEST_DIR}/fully_shredded_list_with_nulls.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (NULL::VARIANT),
        (NULL::VARIANT),
        (['test']::VARIANT),
        (['hello', 'world']::VARIANT),
        (21::VARIANT),
        (NULL::VARIANT)
    ) t(col)
) to '{TEST_DIR}/not_fully_shredded_list.parquet'
--
ok
==
select s.shredding_state, s.has_null, s.has_no_null from (select stats(col) s from '{TEST_DIR}/not_fully_shredded_list.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (['test']::VARIANT),
        (['hello']::VARIANT),
    ) t(col)
) to '{TEST_DIR}/fully_shredded_list_element.parquet'
--
ok
==
select s.min, s.max, s.has_null, s.has_no_null from (select stats(col).fully_shredded.child_stats.stats s from '{TEST_DIR}/fully_shredded_list_element.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (['test']::VARIANT),
        (['hello', NULL, 'world']::VARIANT),
    ) t(col)
) to '{TEST_DIR}/fully_shredded_list_element_with_nulls.parquet'
--
ok
==
select s.min, s.max, s.has_null, s.has_no_null from (select stats(col).fully_shredded.child_stats.stats s from '{TEST_DIR}/fully_shredded_list_element_with_nulls.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (['test']::VARIANT),
        (['hello'::VARIANT, NULL, 21]::VARIANT),
    ) t(col)
) to '{TEST_DIR}/not_fully_shredded_list_element.parquet'
--
ok
==
select s.fully_shredded from (select stats(col) s from '{TEST_DIR}/not_fully_shredded_list_element.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        ({'a': 42, 'b': true}::VARIANT),
        ({'a': 21, 'c': 'test'}::VARIANT),
    ) t(col)
) to '{TEST_DIR}/fully_shredded_object.parquet'
--
ok
==
select s.fully_shredded.a.stats.min, s.fully_shredded.a.stats.max, s.fully_shredded.c.stats.min from (select stats(col) s from '{TEST_DIR}/fully_shredded_object.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (NULL::VARIANT),
        ({'a': 42, 'b': true}::VARIANT),
        (NULL::VARIANT),
        ({'a': 21, 'c': 'test'}::VARIANT),
    ) t(col)
) to '{TEST_DIR}/fully_shredded_object_with_nulls.parquet'
--
ok
==
select s.fully_shredded.a.stats.min, s.fully_shredded.a.stats.max, s.fully_shredded.c.stats.min from (select stats(col) s from '{TEST_DIR}/fully_shredded_object_with_nulls.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (NULL::VARIANT),
        ({'a': 42, 'b': true}::VARIANT),
        (NULL::VARIANT),
        ({'a': 21, 'c': 'test'}::VARIANT),
        (42::VARIANT),
    ) t(col)
) to '{TEST_DIR}/not_fully_shredded_object.parquet'
--
ok
==
select s.fully_shredded from (select stats(col) s from '{TEST_DIR}/not_fully_shredded_object.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (NULL::VARIANT),
        ({'a': 42, 'b': true}::VARIANT),
        (NULL::VARIANT),
        ({'a': 'test', 'c': 'test'}::VARIANT),
    ) t(col)
) to '{TEST_DIR}/not_fully_shredded_object_field.parquet'
--
ok
==
select s.fully_shredded.a, s.fully_shredded.c.stats.min from (select stats(col) s from '{TEST_DIR}/not_fully_shredded_object_field.parquet' limit 1);
--
ok
==
COPY (SELECT * from (VALUES
        (NULL::VARIANT),
        ({'a': 42, 'b': true}::VARIANT),
        (NULL::VARIANT),
        ({'a': NULL, 'c': 'test'}::VARIANT),
    ) t(col)
) to '{TEST_DIR}/fully_shredded_object_field_with_nulls.parquet'
--
ok
==
select s.fully_shredded.a.type, s.fully_shredded.a.stats.min, s.fully_shredded.c.type, s.fully_shredded.c.stats.min from (select stats(col) s from '{TEST_DIR}/fully_shredded_object_field_with_nulls.parquet' limit 1);
--
ok
==
