# source: test/sql/copy/partitioned/skip_partition_column_writes.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

COPY test TO '{TEST_DIR}/no-part-cols' (FORMAT PARQUET, PARTITION_BY (part_col));
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/no-part-cols/part_col=0/*.parquet' ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/no-part-cols-explicit' (FORMAT PARQUET, PARTITION_BY (part_col), WRITE_PARTITION_COLUMNS false);
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/no-part-cols-explicit/part_col=0/*.parquet' ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/no-part-cols2' (FORMAT PARQUET, PARTITION_BY (part_col, value_col));
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/no-part-cols2/part_col=0/value_col=*/*.parquet' ORDER BY value2_col;
--
ok
==
COPY (SELECT * EXCLUDE (part_col), 'prefix-'::VARCHAR || part_col::VARCHAR as part_col FROM test) TO '{TEST_DIR}/no-part-cols3' (FORMAT PARQUET, PARTITION_BY (part_col));
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/no-part-cols3/part_col=prefix-0/*.parquet' ORDER BY value2_col;
--
ok
==
COPY (SELECT 1 AS part_col, 2 AS value_col, 3 AS value2_col, 4 AS value3_col, 5 AS value4_col, 6 AS value5_col, 7 AS value6_col, 8 AS value7_col, 9 AS value8_col, 10 AS value9_col) TO '{TEST_DIR}/no-part-cols4' (FORMAT PARQUET, PARTITION_BY (part_col));
--
ok
==
SELECT part_col, value_col, value2_col, value3_col, value4_col, value5_col, value6_col, value7_col, value8_col, value9_col FROM '{TEST_DIR}/no-part-cols4/part_col=1/*.parquet' ORDER BY 1;
--
ok
==
COPY (SELECT 1 AS part_col, 2 AS value_col, 3 AS value2_col, 4 AS value3_col, 5 AS value4_col, 6 AS value5_col, 7 AS value6_col, 8 AS value7_col, 9 AS value8_col, 10 AS value9_col) TO '{TEST_DIR}/no-part-cols5' (FORMAT PARQUET, PARTITION_BY (value9_col));
--
ok
==
SELECT part_col, value_col, value2_col, value3_col, value4_col, value5_col, value6_col, value7_col, value8_col, value9_col FROM '{TEST_DIR}/no-part-cols5/value9_col=*/*.parquet' ORDER BY 1;
--
ok
==
COPY (SELECT 1 AS part_col, 2 AS value_col, 3 AS value2_col, 4 AS value3_col, 5 AS value4_col, 6 AS value5_col, 7 AS value6_col, 8 AS value7_col, 9 AS value8_col, 10 AS value9_col) TO '{TEST_DIR}/no-part-cols6' (FORMAT PARQUET, PARTITION_BY (value8_col, value9_col));
--
ok
==
SELECT part_col, value_col, value2_col, value3_col, value4_col, value5_col, value6_col, value7_col, value8_col, value9_col FROM '{TEST_DIR}/no-part-cols6/value8_col=*/value9_col=*/*.parquet' ORDER BY 1;
--
ok
==
COPY (SELECT 1 AS part_col, 2 AS value_col, 3 AS value2_col, 4 AS value3_col, 5 AS value4_col, 6 AS value5_col, 7 AS value6_col, 8 AS value7_col, 9 AS value8_col, 10 AS value9_col) TO '{TEST_DIR}/no-part-cols7' (FORMAT PARQUET, PARTITION_BY (value9_col, value8_col, value7_col));
--
ok
==
SELECT part_col, value_col, value2_col, value3_col, value4_col, value5_col, value6_col, value7_col, value8_col, value9_col FROM '{TEST_DIR}/no-part-cols7/value9_col=*/value8_col=*/value7_col=*/*.parquet' ORDER BY 1;
--
ok
==
COPY test TO '{TEST_DIR}/no-part-cols8' (FORMAT PARQUET, PARTITION_BY (part_col, value_col, value2_col));
--
ok
==
COPY test TO '{TEST_DIR}/no-part-cols8' (FORMAT PARQUET, OVERWRITE, PARTITION_BY (part_col, value_col, value2_col), WRITE_PARTITION_COLUMNS);
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/no-part-cols8/part_col=0/value_col=*/value2_col=*/*.parquet' ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/no-part-cols9' (FORMAT PARQUET, PARTITION_BY '*');
--
ok
==
COPY test TO '{TEST_DIR}/no-part-cols9' (FORMAT PARQUET, PARTITION_BY '*', OVERWRITE, WRITE_PARTITION_COLUMNS);
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/no-part-cols9/part_col=0/value_col=*/value2_col=*/*.parquet' ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/no-part-cols9' (FORMAT PARQUET, PARTITION_BY '*', WRITE_PARTITION_COLUMNS false);
--
ok
==
COPY test TO '{TEST_DIR}/csv-no-part-cols' (FORMAT CSV, PARTITION_BY (part_col));
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/csv-no-part-cols/part_col=0/*.csv' ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/csv-no-part-cols-explicit' (FORMAT CSV, PARTITION_BY (part_col), WRITE_PARTITION_COLUMNS false);
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/csv-no-part-cols-explicit/part_col=0/*.csv' ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/csv-no-part-cols2' (FORMAT CSV, PARTITION_BY (part_col, value_col));
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/csv-no-part-cols2/part_col=0/value_col=*/*.csv' ORDER BY value2_col;
--
ok
==
COPY (SELECT * EXCLUDE (part_col), 'prefix-'::VARCHAR || part_col::VARCHAR as part_col FROM test) TO '{TEST_DIR}/csv-no-part-cols3' (FORMAT CSV, PARTITION_BY (part_col));
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/csv-no-part-cols3/part_col=prefix-0/*.csv' ORDER BY value2_col;
--
ok
==
COPY (SELECT 1 AS part_col, 2 AS value_col, 3 AS value2_col, 4 AS value3_col, 5 AS value4_col, 6 AS value5_col, 7 AS value6_col, 8 AS value7_col, 9 AS value8_col, 10 AS value9_col) TO '{TEST_DIR}/csv-no-part-cols4' (FORMAT CSV, PARTITION_BY (part_col));
--
ok
==
SELECT part_col, value_col, value2_col, value3_col, value4_col, value5_col, value6_col, value7_col, value8_col, value9_col FROM '{TEST_DIR}/csv-no-part-cols4/part_col=1/*.csv' ORDER BY 1;
--
ok
==
COPY (SELECT 1 AS part_col, 2 AS value_col, 3 AS value2_col, 4 AS value3_col, 5 AS value4_col, 6 AS value5_col, 7 AS value6_col, 8 AS value7_col, 9 AS value8_col, 10 AS value9_col) TO '{TEST_DIR}/csv-no-part-cols5' (FORMAT CSV, PARTITION_BY (value9_col));
--
ok
==
SELECT part_col, value_col, value2_col, value3_col, value4_col, value5_col, value6_col, value7_col, value8_col, value9_col FROM '{TEST_DIR}/csv-no-part-cols5/value9_col=*/*.csv' ORDER BY 1;
--
ok
==
COPY (SELECT 1 AS part_col, 2 AS value_col, 3 AS value2_col, 4 AS value3_col, 5 AS value4_col, 6 AS value5_col, 7 AS value6_col, 8 AS value7_col, 9 AS value8_col, 10 AS value9_col) TO '{TEST_DIR}/csv-no-part-cols6' (FORMAT CSV, PARTITION_BY (value8_col, value9_col));
--
ok
==
SELECT part_col, value_col, value2_col, value3_col, value4_col, value5_col, value6_col, value7_col, value8_col, value9_col FROM '{TEST_DIR}/csv-no-part-cols6/value8_col=*/value9_col=*/*.csv' ORDER BY 1;
--
ok
==
COPY (SELECT 1 AS part_col, 2 AS value_col, 3 AS value2_col, 4 AS value3_col, 5 AS value4_col, 6 AS value5_col, 7 AS value6_col, 8 AS value7_col, 9 AS value8_col, 10 AS value9_col) TO '{TEST_DIR}/csv-no-part-cols7' (FORMAT CSV, PARTITION_BY (value9_col, value8_col, value7_col));
--
ok
==
SELECT part_col, value_col, value2_col, value3_col, value4_col, value5_col, value6_col, value7_col, value8_col, value9_col FROM '{TEST_DIR}/csv-no-part-cols7/value9_col=*/value8_col=*/value7_col=*/*.csv' ORDER BY 1;
--
ok
==
COPY test TO '{TEST_DIR}/csv-no-part-cols8' (FORMAT CSV, PARTITION_BY (part_col, value_col, value2_col));
--
ok
==
COPY test TO '{TEST_DIR}/csv-no-part-cols8' (FORMAT CSV, PARTITION_BY (part_col, value_col, value2_col), OVERWRITE, WRITE_PARTITION_COLUMNS);
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/csv-no-part-cols8/part_col=0/value_col=*/value2_col=*/*.csv' ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/csv-no-part-cols9' (FORMAT CSV, PARTITION_BY '*');
--
ok
==
COPY test TO '{TEST_DIR}/csv-no-part-cols9' (FORMAT CSV, PARTITION_BY '*', OVERWRITE, WRITE_PARTITION_COLUMNS);
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/csv-no-part-cols9/part_col=0/value_col=*/value2_col=*/*.csv' ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/csv-no-part-cols9' (FORMAT CSV, PARTITION_BY '*', WRITE_PARTITION_COLUMNS false);
--
ok
==
