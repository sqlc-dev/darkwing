# source: test/sql/attach/attach_replay_with_no_wal_writes.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

ATTACH '{TEST_DIR}/wal_writes.db' AS wal_writes;
--
ok
==
CREATE TABLE wal_writes.tbl AS SELECT range AS id, 0 AS v FROM range(5_000);
--
ok
==
DETACH wal_writes;
--
ok
==
SELECT COUNT(*) FROM glob('{TEST_DIR}/wal_writes.db.wal')
--
ok
==
ATTACH '{TEST_DIR}/wal_writes.db' AS wal_writes (RECOVERY_MODE NO_WAL_WRITES);
--
ok
==
INSERT INTO wal_writes.tbl VALUES (42, 42);
--
ok
==
CHECKPOINT wal_writes;
--
ok
==
UPDATE wal_writes.tbl SET v = id;
--
ok
==
