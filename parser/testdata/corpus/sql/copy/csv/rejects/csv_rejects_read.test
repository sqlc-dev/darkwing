# source: test/sql/copy/csv/rejects/csv_rejects_read.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

SELECT * FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/bad.csv',
    columns = {'col0': 'INTEGER', 'col1': 'INTEGER', 'col2': 'VARCHAR'},
    store_rejects = true, auto_detect=true);
--
ok
==
SELECT * EXCLUDE (scan_id) FROM reject_errors;
--
ok
==
SELECT * FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/bad2.csv',
    columns = {'col0': 'INTEGER', 'col1': 'INTEGER', 'col2': 'INTEGER'},
    store_rejects = true, auto_detect=false);
--
ok
==
SELECT * EXCLUDE (scan_id) FROM reject_errors ORDER BY ALL;
--
ok
==
SELECT * FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/bad*.csv',
    columns = {'col0': 'INTEGER', 'col1': 'INTEGER', 'col2': 'VARCHAR'},
    store_rejects = true, auto_detect=false);
--
ok
==
SELECT * FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/bad*.csv',
    columns = {'col0': 'INTEGER', 'col1': 'INTEGER', 'col2': 'VARCHAR'},
    store_rejects = true,rejects_limit=2, ignore_errors=true, auto_detect=false);
--
ok
==
SELECT COUNT(*) FROM reject_errors
--
ok
==
SELECT SUM(num) FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/big_bad.csv',
    columns = {'num': 'INTEGER', 'str': 'VARCHAR'},
    store_rejects = true, auto_detect=false);
--
ok
==
SELECT SUM(num) FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/big_bad2.csv',
    columns = {'num': 'INTEGER', 'str': 'VARCHAR'},
    store_rejects = true,  auto_detect=false)
--
ok
==
SELECT SUM(num) FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/big_*.csv',
    columns = {'num': 'INTEGER', 'str': 'VARCHAR'},
    store_rejects = true, auto_detect=false);
--
ok
==
SELECT *
FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/small1.csv',
    columns = {'num': 'INTEGER', 'str': 'VARCHAR'},
    store_rejects = true) as L
JOIN read_csv(
    '{DATA_DIR}/csv/error/mismatch/small2.csv',
    columns = {'num': 'INTEGER', 'str': 'VARCHAR'},
   store_rejects = true) as R
ON L.num = R.num;
--
ok
==
SELECT * EXCLUDE (scan_id, file_id) FROM reject_scans ORDER BY ALL;
--
ok
==
SELECT * EXCLUDE (scan_id, file_id) FROM reject_errors ORDER BY ALL;
--
ok
==
SELECT *
FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/small1.csv',
    columns = {'num': 'INTEGER', 'str': 'VARCHAR'},
    store_rejects = true) as L
JOIN read_csv(
    '{DATA_DIR}/csv/error/mismatch/small2.csv',
    columns = {'num': 'INTEGER', 'str': 'VARCHAR'},
    store_rejects = true, rejects_limit=1) as R
ON L.num = R.num;
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/dr_who.csv', columns={ 'date': 'DATE', 'datetime': 'TIMESTAMPTZ', 'time': 'TIME', 'timestamp': 'TIMESTAMP', 'time_tz': 'TIMETZ' }, store_rejects=true);
--
ok
==
SELECT * EXCLUDE (scan_id, file_id) FROM reject_errors ORDER BY column_name;
--
ok
==
FROM read_csv('{DATA_DIR}/csv/comments/error.csv', store_rejects = true, comment = '#');
--
ok
==
FROM read_csv('{DATA_DIR}/csv/error.csv', store_rejects=1, strict_mode=True);
--
ok
==
