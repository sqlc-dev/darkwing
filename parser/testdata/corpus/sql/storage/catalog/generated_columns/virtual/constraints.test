# source: test/sql/storage/catalog/generated_columns/virtual/constraints.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE tbl (
	gcol_x AS (x),
	gcol_y AS (y),
	y INTEGER,
	x TEXT
	CHECK (y > 5)
);
--
ok
==
INSERT INTO tbl VALUES (5, 'test');
--
ok
==
INSERT INTO tbl VALUES (6,'test');
--
ok
==
CREATE TABLE tbl (
	gcol_x AS (x),
	gcol_y AS (y),
	y INTEGER CHECK (y > 5),
	x TEXT
);
--
ok
==
CREATE TABLE tbl (
	gcol_x AS (x),
	gcol_y AS (y),
	y INTEGER UNIQUE,
	x TEXT PRIMARY KEY
);
--
ok
==
CREATE TABLE tbl (
	gcol_x AS (x),
	gcol_y AS (y),
	y INTEGER,
	x TEXT,
	PRIMARY KEY (y),
	UNIQUE (x)
);
--
ok
==
CREATE TABLE base (
	price INTEGER PRIMARY KEY
);
--
ok
==
CREATE TABLE tbl (
	gcol_nest AS (gcol),
	gcol AS (x),
	x INTEGER,
	FOREIGN KEY (x) REFERENCES base (price)
);
--
ok
==
INSERT INTO base VALUES (5);
--
ok
==
DROP TABLE base;
--
ok
==
CREATE TABLE tbl (
	gcol2 AS (gcol1),
	price INTEGER DEFAULT (5),
	gcol1 AS (price),
);
--
ok
==
ALTER TABLE tbl DROP COLUMN gcol1;
--
ok
==
ALTER TABLE tbl DROP COLUMN gcol2;
--
ok
==
