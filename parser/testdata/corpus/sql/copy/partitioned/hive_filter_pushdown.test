# source: test/sql/copy/partitioned/hive_filter_pushdown.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

COPY (SELECT i::VARCHAR as a, (i*10)::VARCHAR as b, (i*100)::VARCHAR as c from range(0,10) tbl(i)) TO '{TEST_DIR}/hive_pushdown_bug' (FORMAT PARQUET, PARTITION_BY c);
--
ok
==
explain SELECT * FROM parquet_scan('{TEST_DIR}/hive_pushdown_bug/*/*.parquet', HIVE_PARTITIONING=1) where a > b;
--
ok
==
explain SELECT * FROM parquet_scan('{TEST_DIR}/hive_pushdown_bug/*/*.parquet') where a > b;
--
ok
==
explain SELECT * FROM parquet_scan('{TEST_DIR}/hive_pushdown_bug/*/*.parquet', HIVE_PARTITIONING=1) where a::VARCHAR > c::VARCHAR;
--
ok
==
explain SELECT * FROM parquet_scan('{TEST_DIR}/hive_pushdown_bug/*/*.parquet') where a::VARCHAR > c::VARCHAR;
--
ok
==
explain SELECT * FROM parquet_scan('{TEST_DIR}/hive_pushdown_bug/*/*.parquet', HIVE_PARTITIONING=1, HIVE_TYPES_AUTOCAST=1) where c=500;
--
ok
==
explain SELECT * FROM parquet_scan('{TEST_DIR}/hive_pushdown_bug/*/*.parquet', HIVE_PARTITIONING=1, HIVE_TYPES_AUTOCAST=1) where c=500 and b='20';
--
ok
==
COPY (SELECT i::VARCHAR as a, (i*10)::VARCHAR as b, (i*100)::VARCHAR as c from range(0,10) tbl(i)) TO '{TEST_DIR}/hive_pushdown_bug_csv' (FORMAT CSV, PARTITION_BY c);
--
ok
==
explain SELECT * FROM read_csv_auto('{TEST_DIR}/hive_pushdown_bug_csv/*/*.csv', HIVE_PARTITIONING=1, HIVE_TYPES_AUTOCAST=1, names=['a','b','c']) where c=500;
--
ok
==
