# source: test/sql/filter/test_variant_filter.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

WITH cte as (
	select '12'::VARIANT a
)
select IF(a == 12, 1, 0) from cte
--
ok
==
WITH cte as (
	select '12'::VARIANT a
)
select IF(a == [1,2,3], 1, 0) from cte
--
ok
==
WITH cte as (
	select '[1,2,3]'::VARIANT a
)
select IF(a == [1,2,3], 1, 0) from cte
--
ok
==
WITH cte as (
	select [1,2,3]::VARIANT a
)
select IF(a == [3, 2, 1], 1, 0) from cte
--
ok
==
WITH cte as (
	select [1,2,3]::VARIANT a
)
select IF(a == [1,2,3], 1, 0) from cte
--
ok
==
WITH cte as (
	select [1::VARIANT,'2',3]::VARIANT a
)
select IF(a == [1,2,3], 1, 0) from cte
--
ok
==
WITH cte as (
	select {a: 21, b: [1,2,3]}::VARIANT a
)
select IF(a == [1,2,3], 1, 0) from cte
--
ok
==
WITH cte as (
	select {a: 21, b: [1,2,3]}::VARIANT a
)
select IF(a == {b: [1,2,3], a: 21}, 1, 0) from cte
--
ok
==
WITH cte as (
	select {a: 21, b: [1,2,3]}::VARIANT a
)
select IF(a < [1,2,3], 1, 0) from cte
--
ok
==
WITH cte as (
	select {a: 21, b: [1,2,3]}::VARIANT a
)
select IF(a > [1,2,3], 1, 0) from cte
--
ok
==
