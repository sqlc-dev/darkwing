# source: test/sql/window/test_empty_frames.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE t1 (id INTEGER, ch CHAR(1)) ;
--
ok
==
INSERT INTO t1 VALUES (1, 'A');
--
ok
==
INSERT INTO t1 VALUES (2, 'B');
--
ok
==
INSERT INTO t1 VALUES (NULL, 'B');
--
ok
==
SELECT id, {agg} OVER (PARTITION BY ch ORDER BY id ROWS BETWEEN 1 FOLLOWING AND 2 FOLLOWING) 
FROM t1
ORDER BY 1;
--
error: Parser Error: syntax error at or near "}"
==
SELECT {agg} OVER (ROWS BETWEEN 1 FOLLOWING AND 1 PRECEDING) AS actual
FROM range(1);
--
error: Parser Error: syntax error at or near "}"
==
SELECT id, count(*) OVER (PARTITION BY ch ORDER BY id ROWS BETWEEN 3 FOLLOWING AND 1 FOLLOWING)
FROM t1
ORDER BY 1;
--
ok
==
SELECT {agg} OVER (ROWS BETWEEN 1 FOLLOWING AND 1 PRECEDING EXCLUDE TIES) AS actual
FROM range(1);
--
error: Parser Error: syntax error at or near "}"
==
SELECT id, lead(id ORDER BY id) OVER (ORDER BY ch ROWS BETWEEN 3 FOLLOWING AND 1 FOLLOWING)
FROM t1
ORDER BY 1;
--
ok
==
SELECT id, rank() OVER (ORDER BY id ROWS BETWEEN 3 FOLLOWING AND 1 FOLLOWING)
FROM t1
ORDER BY 1;
--
ok
==
SELECT id, {func}(ORDER BY id) OVER (ORDER BY ch ROWS BETWEEN 3 FOLLOWING AND 1 FOLLOWING)
FROM t1
ORDER BY 1;
--
error: Parser Error: syntax error at or near "}"
==
SELECT id, ntile(2 ORDER BY id) OVER (ORDER BY ch ROWS BETWEEN 3 FOLLOWING AND 1 FOLLOWING)
FROM t1
ORDER BY 1;
--
ok
==
SELECT id, rank(ORDER BY id) OVER (ORDER BY ch ROWS BETWEEN 3 FOLLOWING AND 1 FOLLOWING)
FROM t1
ORDER BY 1;
--
ok
==
SELECT id, {func}(ORDER BY id) OVER (ORDER BY ch ROWS BETWEEN 1 FOLLOWING AND CURRENT ROW)
FROM t1
ORDER BY 1;
--
error: Parser Error: syntax error at or near "}"
==
SELECT id, ntile(2 ORDER BY id) OVER (ORDER BY ch ROWS BETWEEN 1 FOLLOWING AND CURRENT ROW)
FROM t1
ORDER BY 1;
--
ok
==
SELECT id, rank(ORDER BY id) OVER (ORDER BY ch ROWS BETWEEN 1 FOLLOWING AND CURRENT ROW)
FROM t1
ORDER BY 1;
--
ok
==
SELECT id, {agg}(id) OVER (PARTITION BY ch ORDER BY id ROWS BETWEEN 1 FOLLOWING AND 2 FOLLOWING) 
FROM t1
ORDER BY 1;
--
error: Parser Error: syntax error at or near "}"
==
SELECT id, {agg}(id, 0.5) OVER (PARTITION BY ch ORDER BY id ROWS BETWEEN 1 FOLLOWING AND 2 FOLLOWING) 
FROM t1
ORDER BY 1;
--
error: Parser Error: syntax error at or near "}"
==
SELECT id, {agg}(id, ch) OVER (PARTITION BY ch ORDER BY id ROWS BETWEEN 1 FOLLOWING AND 2 FOLLOWING) 
FROM t1
ORDER BY 1;
--
error: Parser Error: syntax error at or near "}"
==
SELECT id, {agg}(id > 0) OVER (PARTITION BY ch ORDER BY id ROWS BETWEEN 1 FOLLOWING AND 2 FOLLOWING) 
FROM t1
ORDER BY 1;
--
error: Parser Error: syntax error at or near "}"
==
SELECT id, {agg}(id, id) OVER (PARTITION BY ch ORDER BY id ROWS BETWEEN 1 FOLLOWING AND 2 FOLLOWING) 
FROM t1
ORDER BY 1;
--
error: Parser Error: syntax error at or near "}"
==
SELECT id, string_agg(id, ' ') OVER (PARTITION BY ch ORDER BY id ROWS BETWEEN 1 FOLLOWING AND 2 FOLLOWING) 
FROM t1
ORDER BY 1;
--
ok
==
SELECT id, bitstring_agg(id, 1, 3) OVER (PARTITION BY ch ORDER BY id ROWS BETWEEN 1 FOLLOWING AND 2 FOLLOWING) 
FROM t1
ORDER BY 1;
--
ok
==
