# source: test/sql/copy/parquet/writer/write_struct.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE struct AS SELECT * FROM (VALUES
	({'a': 42, 'b': 84}),
	({'a': 33, 'b': 32}),
	({'a': 42, 'b': 27})
) tbl(i);
--
ok
==
COPY struct TO '{TEST_DIR}/test_struct.parquet' (FORMAT 'parquet');
--
ok
==
SELECT * FROM parquet_scan('{TEST_DIR}/test_struct.parquet');
--
ok
==
CREATE TABLE struct_nulls AS SELECT * FROM (VALUES
	({'a': 42, 'b': 84}),
	({'a': NULL, 'b': 32}),
	(NULL),
	({'a': 42, 'b': NULL})
) tbl(i);
--
ok
==
COPY struct_nulls TO '{TEST_DIR}/test_struct_nulls.parquet' (FORMAT 'parquet');
--
ok
==
SELECT * FROM parquet_scan('{TEST_DIR}/test_struct_nulls.parquet');
--
ok
==
CREATE TABLE struct_nested AS SELECT * FROM (VALUES
	({'a': {'x': 3, 'x1': 22}, 'b': {'y': 27, 'y1': 44}}),
	({'a': {'x': 9, 'x1': 26}, 'b': {'y': 1, 'y1': 999}}),
	({'a': {'x': 17, 'x1': 23}, 'b': {'y': 3, 'y1': 9999}})
) tbl(i);
--
ok
==
COPY struct_nested TO '{TEST_DIR}/struct_nested.parquet' (FORMAT 'parquet');
--
ok
==
SELECT * FROM parquet_scan('{TEST_DIR}/struct_nested.parquet');
--
ok
==
CREATE TABLE struct_nested_null AS SELECT * FROM (VALUES
	({'a': {'x': 3, 'x1': 22}, 'b': {'y': NULL, 'y1': 44}}),
	({'a': {'x': NULL, 'x1': 26}, 'b': {'y': 1, 'y1': NULL}}),
	({'a': {'x': 17, 'x1': NULL}, 'b': {'y': 3, 'y1': 9999}}),
	(NULL),
	({'a': NULL, 'b': NULL})
) tbl(i);
--
ok
==
COPY struct_nested_null TO '{TEST_DIR}/struct_nested_null.parquet' (FORMAT 'parquet');
--
ok
==
SELECT * FROM parquet_scan('{TEST_DIR}/struct_nested_null.parquet');
--
ok
==
CREATE TABLE single_struct AS SELECT * FROM (VALUES
	({'a': 42}),
	({'a': 33}),
	({'a': 42})
) tbl(i);
--
ok
==
COPY single_struct TO '{TEST_DIR}/single_struct.parquet' (FORMAT 'parquet');
--
ok
==
SELECT * FROM parquet_scan('{TEST_DIR}/single_struct.parquet');
--
ok
==
CREATE TABLE single_struct_null AS SELECT * FROM (VALUES
	({'a': 42}),
	({'a': NULL}),
	(NULL)
) tbl(i);
--
ok
==
COPY single_struct_null TO '{TEST_DIR}/single_struct_null.parquet' (FORMAT 'parquet');
--
ok
==
SELECT * FROM parquet_scan('{TEST_DIR}/single_struct_null.parquet');
--
ok
==
CREATE TABLE nested_single_struct AS SELECT * FROM (VALUES
	({'a': {'b': 42}}),
	({'a': {'b': NULL}}),
	({'a': NULL}),
	(NULL)
) tbl(i);
--
ok
==
COPY nested_single_struct TO '{TEST_DIR}/nested_single_struct.parquet' (FORMAT 'parquet');
--
ok
==
SELECT * FROM parquet_scan('{TEST_DIR}/nested_single_struct.parquet');
--
ok
==
