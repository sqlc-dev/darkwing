# source: test/sql/copy/csv/rejects/csv_incorrect_columns_amount_rejects.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

SELECT * FROM read_csv(
    '{DATA_DIR}/csv/rejects/incorrect_columns/few_columns.csv',
    columns = {'a': 'INTEGER', 'b': 'INTEGER', 'c': 'INTEGER', 'd': 'INTEGER'},
    store_rejects=true, auto_detect=false, header = 1);
--
ok
==
SELECT * EXCLUDE (scan_id) FROM reject_errors order by all;
--
ok
==
DROP TABLE reject_errors;
--
ok
==
DROP TABLE reject_scans;
--
ok
==
SELECT * FROM read_csv(
    '{DATA_DIR}/csv/rejects/incorrect_columns/many_columns.csv',
    columns = {'a': 'INTEGER', 'b': 'INTEGER', 'c': 'INTEGER', 'd': 'INTEGER'},
    store_rejects=true, auto_detect=false, header = 1, strict_mode=True);
--
ok
==
SELECT * FROM read_csv(
    '{DATA_DIR}/csv/rejects/incorrect_columns/mix_columns.csv',
    columns = {'a': 'INTEGER', 'b': 'INTEGER', 'c': 'INTEGER', 'd': 'INTEGER'},
    store_rejects=true, auto_detect=false, header = 1, strict_mode=True);
--
ok
==
SELECT * FROM read_csv(
    '{DATA_DIR}/csv/rejects/incorrect_columns/small_mix.csv',
    columns = {'a': 'INTEGER', 'b': 'INTEGER', 'c': 'INTEGER', 'd': 'INTEGER'},
    store_rejects=true, auto_detect=false, header = 1, strict_mode=True);
--
ok
==
SELECT * EXCLUDE (scan_id) FROM reject_errors order by all
--
ok
==
SELECT * FROM read_csv(
    '{DATA_DIR}/csv/rejects/incorrect_columns/*.csv',
    columns = {'a': 'INTEGER', 'b': 'INTEGER', 'c': 'INTEGER', 'd': 'INTEGER'},
   store_rejects=true, auto_detect=false, header = 1, strict_mode=True);
--
ok
==
