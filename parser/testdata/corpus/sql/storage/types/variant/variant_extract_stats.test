# source: test/sql/storage/types/variant/variant_extract_stats.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

SET force_variant_shredding = 'VARCHAR[]';
--
ok
==
insert into tbl select
	['hello', 'world']
from range(5) t(i)
--
ok
==
select s.fully_shredded.stats.min, s.fully_shredded.stats.max from (select stats(col[1]) s from tbl limit 1);
--
ok
==
insert into tbl select 42::INTEGER from range(5) t(i)
--
ok
==
select s.fully_shredded from (select stats(col) s from tbl limit 1);
--
ok
==
select stats(col[1]).shredding_state from tbl offset 5 limit 1;
--
ok
==
delete from tbl where not variant_typeof(col).starts_with('ARRAY')
--
ok
==
insert into tbl select
	[42, 21, 1337]
from range(5)
--
ok
==
delete from tbl;
--
ok
==
insert into tbl select {'a': 42, 'b': false} from range(5)
--
ok
==
select stats(col.a).shredding_state from tbl limit 1;
--
ok
==
SET force_variant_shredding = 'STRUCT(a VARCHAR, b INTEGER)';
--
ok
==
insert into tbl select {'a': 'test', 'b': false}
--
ok
==
select s.fully_shredded.stats.min, s.fully_shredded.stats.max_string_length from (select stats(col.a) s from tbl limit 1);
--
ok
==
select stats(col.b).shredding_state from tbl limit 1;
--
ok
==
select s.fully_shredded.a.stats.min from (select stats(col) s from tbl limit 1);
--
ok
==
insert into tbl select {'a': 42::INTEGER, 'b': false}
--
ok
==
select stats(col).shredding_state from tbl limit 1;
--
ok
==
insert into tbl select {'a': 'test', 'c': [42], 'b': false}
--
ok
==
insert into tbl select {'b': 1337::INTEGER}
--
ok
==
insert into tbl select 42::INTEGER
--
ok
==
select stats(col.c).shredding_state from tbl order by rowid offset 2 limit 1;
--
ok
==
SET force_variant_shredding = 'STRUCT(a STRUCT(b VARCHAR, c VARCHAR))';
--
ok
==
insert into tbl select {'a': {'b': 'hello', 'c': 'world'}} from range(5)
--
ok
==
select s.fully_shredded.stats.min, s.fully_shredded.stats.max from (select stats(col.a.b) s from tbl limit 1);
--
ok
==
select s.fully_shredded.stats.min, s.fully_shredded.stats.max from (select stats(col.a.c) s from tbl limit 1);
--
ok
==
select stats(col.a.b).shredding_state from tbl limit 1;
--
ok
==
