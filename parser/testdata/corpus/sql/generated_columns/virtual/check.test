# source: test/sql/generated_columns/virtual/check.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE unit (
	price INTEGER,
	amount_sold AS (price) CHECK (price > 5),
);
--
ok
==
CREATE TABLE tbl (
	price INTEGER,
	amount_sold AS (price) CHECK (amount_sold > 5)
);
--
ok
==
CREATE TABLE tbl (
	price INTEGER,
	amount_sold AS (price),
	CHECK (amount_sold > price)
);
--
ok
==
CREATE TABLE chk (
	g1 AS (x),
	g2 AS (x),
	x INTEGER,
	CHECK (x > 5)
);
--
ok
==
INSERT INTO chk VALUES (6);
--
ok
==
INSERT INTO chk VALUES (3);
--
ok
==
