# source: test/sql/pragma/profiling/test_custom_profiling_disable_metrics.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

SET tracked_metrics = ['operator.extra_info', 'operator.intermediate_rows', 'operator.timing', 'query.total_time'];
--
ok
==
SELECT query.cpu_time FROM metrics_output;
--
ok
==
SELECT operator[1].extra_info, query.total_time FROM metrics_output;
--
ok
==
SET tracked_metrics = ['query.sql', 'query.cpu_time', 'operator.extra_info', 'query.total_intermediate_rows', 'query.total_rows_scanned'];
--
ok
==
SELECT
	CASE WHEN query.cpu_time > 0 THEN 'true'
    ELSE 'false' END
FROM metrics_output;
--
ok
==
SELECT
	CASE WHEN query.total_intermediate_rows > 0 THEN 'true'
	ELSE 'false' END
FROM metrics_output;
--
ok
==
SELECT
	CASE WHEN query.total_rows_scanned > 0 THEN 'true'
	ELSE 'false' END
FROM metrics_output;
--
ok
==
SELECT
	CASE WHEN EXISTS(
		SELECT
			1
		FROM information_schema.columns
		WHERE table_name = 'metrics_output' AND column_name = 'query'
	) THEN 'true'
	ELSE 'false' END;
--
ok
==
SELECT
	query.sql
FROM metrics_output;
--
ok
==
SET tracked_metrics = [];
--
ok
==
PRAGMA profiling_output = '{TEST_DIR}/profiling_output_no_query.json';
--
error: Parser Error: Profiler file type ({TEST_DIR}/profiling_output_no_query.json) must either have the same file extension as the profiling output type (query_tree), or be a '.txt' file. Set "enable_profiling = 'json'" first.
==
CREATE OR REPLACE TABLE metrics_output AS SELECT * FROM '{TEST_DIR}/profiling_output_no_query.json';
--
ok
==
