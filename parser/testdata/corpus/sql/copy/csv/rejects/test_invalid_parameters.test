# source: test/sql/copy/csv/rejects/test_invalid_parameters.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

SELECT * FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/bad.csv',
    columns = {'col0': 'INTEGER', 'col1': 'INTEGER', 'col2': 'VARCHAR'},
    ignore_errors=false,
    store_rejects=true
)
--
ok
==
SELECT * FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/bad.csv',
    columns = {'col0': 'INTEGER', 'col1': 'INTEGER', 'col2': 'VARCHAR'},
    ignore_errors=true,
    rejects_table='')
--
ok
==
SELECT * FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/bad.csv',
    columns = {'col0': 'INTEGER', 'col1': 'INTEGER', 'col2': 'VARCHAR'},
    ignore_errors=true,
    rejects_table='csv_rejects_table',
    union_by_name=true)
--
ok
==
SELECT * FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/bad.csv',
    columns = {'col0': 'INTEGER', 'col1': 'INTEGER', 'col2': 'VARCHAR'},
    ignore_errors=true,
    rejects_limit=10)
--
ok
==
SELECT * FROM read_csv(
    '{DATA_DIR}/csv/error/mismatch/bad.csv',
    columns = {'col0': 'INTEGER', 'col1': 'INTEGER', 'col2': 'VARCHAR'},
    ignore_errors=true,
    rejects_table='csv_rejects_table',
    rejects_limit=-1)
--
ok
==
SELECT * FROM read_csv_auto(
    '{DATA_DIR}/csv/error/mismatch/bad.csv',
    ignore_errors=false,
    rejects_table='csv_rejects_table'
)
--
ok
==
SELECT * FROM read_csv_auto(
    '{DATA_DIR}/csv/error/mismatch/bad.csv',
    ignore_errors=true,
    rejects_table='')
--
ok
==
SELECT * FROM read_csv_auto(
    '{DATA_DIR}/csv/error/mismatch/bad.csv',
    ignore_errors=true,
    rejects_table='csv_rejects_table',
    union_by_name=true)
--
ok
==
SELECT * FROM read_csv_auto(
    '{DATA_DIR}/csv/error/mismatch/bad.csv',
    ignore_errors=true,
    rejects_limit=10)
--
ok
==
SELECT * FROM read_csv_auto(
    '{DATA_DIR}/csv/error/mismatch/bad.csv',
    ignore_errors=true,
    rejects_table='csv_rejects_table',
    rejects_limit=-1)
--
ok
==
SELECT typeof(first(column0)), typeof(first(column1)), COUNT(*) FROM read_csv_auto(
    '{DATA_DIR}/csv/error/mismatch/big_bad*.csv',
    sample_size=3000,
    rejects_table='csv_rejects_table',
    ignore_errors=true, header = 0);
--
ok
==
