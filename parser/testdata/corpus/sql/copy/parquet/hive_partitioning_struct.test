# source: test/sql/copy/parquet/hive_partitioning_struct.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

COPY (
	SELECT
		i//50 id,
		{'a': i, 'b': 21} s
	FROM range(100) t(i)
) TO '{TEST_DIR}/hive_partitioned_struct_col' (FORMAT PARQUET, PARTITION_BY (id))
--
ok
==
SELECT * FROM read_parquet('{TEST_DIR}/hive_partitioned_struct_col/**/*.parquet', hive_partitioning=1) WHERE s.a=42
--
ok
==
SELECT s.a FROM read_parquet('{TEST_DIR}/hive_partitioned_struct_col/**/*.parquet', hive_partitioning=1) WHERE s.a=42
--
ok
==
COPY (SELECT i id, {'a': i//2} s FROM range(100) t(i)) TO '{TEST_DIR}/hive_partitioned_struct' (FORMAT PARQUET, PARTITION_BY (s))
--
ok
==
SELECT * FROM read_parquet('{TEST_DIR}/hive_partitioned_struct/**/*.parquet', hive_partitioning=1, hive_types={'s': 'STRUCT(a INT)'}) WHERE s.a=42 ORDER BY ALL
--
ok
==
