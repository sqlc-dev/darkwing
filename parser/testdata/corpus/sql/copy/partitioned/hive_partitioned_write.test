# source: test/sql/copy/partitioned/hive_partitioned_write.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE test as SELECT i%2 as part_col, (i+1)%5 as value_col, i as value2_col from range(0,10) tbl(i);
--
ok
==
COPY test TO '{TEST_DIR}/partitioned1' (FORMAT PARQUET, PARTITION_BY (part_col));
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/partitioned1/part_col=0/*.parquet' ORDER BY value2_col;
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/partitioned1/part_col=1/*.parquet' ORDER BY value2_col;
--
ok
==
COPY (SELECT * EXCLUDE (part_col), 'prefix-'::VARCHAR || part_col::VARCHAR as part_col FROM test) TO '{TEST_DIR}/partitioned2' (FORMAT PARQUET, PARTITION_BY (part_col));
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/partitioned2/part_col=prefix-0/*.parquet' ORDER BY value2_col;
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/partitioned2/part_col=prefix-1/*.parquet' ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/partitioned3' (FORMAT PARQUET, PARTITION_BY '*', WRITE_PARTITION_COLUMNS);
--
ok
==
SELECT min(value2_col) as min_val
FROM parquet_scan('{TEST_DIR}/partitioned3/part_col=*/value_col=*/value2_col=*/*.parquet', FILENAME=1)
GROUP BY filename
ORDER BY min_val
--
ok
==
COPY test TO '{TEST_DIR}/partitioned4' (FORMAT PARQUET, PARTITION_BY part_col);
--
ok
==
SELECT part_col, value_col, value2_col FROM parquet_scan('{TEST_DIR}/partitioned4/part_col=*/*.parquet', HIVE_PARTITIONING=1) WHERE part_col=0 ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/partitioned5/' (FORMAT PARQUET, PARTITION_BY part_col);
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/partitioned5/part_col=0/*.parquet' ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/partitioned6' (FORMAT PARQUET, PARTITION_BY part_col, USE_TMP_FILE TRUE);
--
ok
==
COPY test TO '{TEST_DIR}/partitioned6' (FORMAT PARQUET, PARTITION_BY part_col, PER_THREAD_OUTPUT TRUE);
--
ok
==
COPY test TO '{TEST_DIR}/partitioned7' (FORMAT CSV, PARTITION_BY part_col);
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/partitioned7/part_col=0/*.csv' ORDER BY value2_col;
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/partitioned7/part_col=1/*.csv' ORDER BY value2_col;
--
ok
==
COPY test TO '{TEST_DIR}/partitioned8' (FORMAT PARQUET, PARTITION_BY pArt_cOl);
--
ok
==
SELECT part_col, value_col, value2_col FROM '{TEST_DIR}/partitioned8/part_col=0/*.parquet' ORDER BY value2_cOl;
--
ok
==
COPY test TO '{TEST_DIR}/partitioned9' (FORMAT PARQUET, PARTITION_BY (part_col, value_col));
--
ok
==
SELECT min(value2_col) as min_val
FROM parquet_scan('{TEST_DIR}/partitioned9/part_col=*/value_col=*/*.parquet', FILENAME=1)
GROUP BY filename
ORDER BY min_val
--
ok
==
COPY test TO '{TEST_DIR}/partitioned10' (FORMAT PARQUET, PARTITION_BY (value_col, part_col));
--
ok
==
SELECT min(value2_col) as min_val
FROM parquet_scan('{TEST_DIR}/partitioned10/value_col=*/part_col=*/*.parquet', FILENAME=1)
GROUP BY filename
ORDER BY min_val
--
ok
==
