# source: test/sql/copy/partitioned/partition_issue_6304.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

copy (select NULL as i, NULL as j from range(100000)) to '{TEST_DIR}/issue6304_null' (format parquet, partition_by(i), overwrite_or_ignore);
--
ok
==
copy (select 1 as i, 2 as j from range(100000)) to '{TEST_DIR}/issue6304_constant' (format parquet, partition_by(i), overwrite_or_ignore);
--
ok
==
copy (select NULL as i from range(100000)) to '{TEST_DIR}/issue6304_null' (format parquet, partition_by(i), overwrite_or_ignore, write_partition_columns);
--
ok
==
copy (select 1 as i from range(100000)) to '{TEST_DIR}/issue6304_constant' (format parquet, partition_by(i), overwrite_or_ignore, write_partition_columns);
--
ok
==
