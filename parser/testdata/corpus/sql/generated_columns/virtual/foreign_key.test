# source: test/sql/generated_columns/virtual/foreign_key.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE tbl1 (
	a INTEGER UNIQUE,
);
--
ok
==
CREATE TABLE tbl2 (
	a INTEGER,
	b AS (a) REFERENCES tbl1(a),
);
--
ok
==
CREATE TABLE tbl2 (
	a INTEGER,
	b AS (a),
	FOREIGN KEY (b) REFERENCES tbl1 (a)
);
--
ok
==
CREATE TABLE tbl2 (
	price INTEGER,
	also_price AS (price) UNIQUE
);
--
ok
==
CREATE TABLE tbl2 (
	price INTEGER,
	also_price AS (price)
);
--
ok
==
CREATE TABLE tbl3 (
	a INTEGER,
	FOREIGN KEY (a) REFERENCES tbl2 (also_price)
);
--
ok
==
CREATE TABLE a (
	a_gen1 AS (a_reg),
	a_gen2 AS (a_reg),
	a_reg INTEGER,
	PRIMARY KEY (a_reg)
);
--
ok
==
CREATE TABLE b (
	gen1 AS (price),
	gen2 AS (price),
	price INTEGER,
	FOREIGN KEY (price) REFERENCES a (a_reg)
);
--
ok
==
INSERT INTO a VALUES (5);
--
ok
==
