# source: test/sql/storage/adaptive_segment_allocation.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

SET initial_column_segment_size = 7
--
ok
==
SET initial_column_segment_size = 9
--
ok
==
SET initial_column_segment_size = 8
--
ok
==
CREATE TABLE step_test (i INT NOT NULL)
--
ok
==
INSERT INTO step_test VALUES (1), (2), (3), (4), (5)
--
ok
==
SELECT segment_id, count
FROM pragma_storage_info('step_test')
WHERE column_name = 'i' AND segment_type = 'INTEGER'
ORDER BY segment_id
--
ok
==
DROP TABLE step_test
--
ok
==
CREATE TABLE mem_adaptive (a INT NOT NULL, b INT NOT NULL, c INT NOT NULL, d INT NOT NULL, e INT NOT NULL)
--
ok
==
INSERT INTO mem_adaptive VALUES (1, 2, 3, 4, 5)
--
ok
==
SELECT CASE WHEN memory_usage_bytes < 2000
            THEN 'ok'
            ELSE error(concat('expected < 2000 bytes, got ', memory_usage_bytes))
       END
FROM duckdb_memory()
WHERE tag = 'IN_MEMORY_TABLE'
--
ok
==
DROP TABLE mem_adaptive
--
ok
==
CREATE TABLE overflow_test (i BIGINT NOT NULL)
--
ok
==
INSERT INTO overflow_test SELECT range FROM range(100)
--
ok
==
SELECT COUNT(*) FROM overflow_test
--
ok
==
SELECT SUM(i) FROM overflow_test
--
ok
==
SELECT MIN(i) = 0 AND MAX(i) = 99 FROM overflow_test
--
ok
==
DROP TABLE overflow_test
--
ok
==
CREATE TABLE validity_test (i INTEGER)
--
ok
==
INSERT INTO validity_test
    SELECT CASE WHEN range % 7 = 6 THEN NULL ELSE range END
    FROM range(100)
--
ok
==
SELECT COUNT(*) FROM validity_test
--
ok
==
SELECT COUNT(*) FILTER (WHERE i IS NULL) FROM validity_test
--
ok
==
SELECT COUNT(*) FILTER (WHERE i IS NOT NULL) FROM validity_test
--
ok
==
SELECT COUNT(*) FROM (
    SELECT ROW_NUMBER() OVER () - 1 AS rn, i FROM validity_test
) WHERE (rn % 7 = 6) != (i IS NULL)
--
ok
==
CREATE TABLE string_test (s VARCHAR)
--
ok
==
INSERT INTO string_test
    SELECT CASE WHEN range % 2 = 0
                THEN range::VARCHAR
                ELSE repeat('x', 50) || range::VARCHAR
           END
    FROM range(100)
--
ok
==
SELECT COUNT(*) FROM string_test
--
ok
==
SELECT COUNT(*) FILTER (WHERE len(s) <= 12) FROM string_test
--
ok
==
SELECT COUNT(*) FILTER (WHERE len(s) > 50) FROM string_test
--
ok
==
SELECT s FROM string_test WHERE s = '42'
--
ok
==
SELECT len(s) FROM string_test WHERE s = repeat('x', 50) || '1'
--
ok
==
DROP TABLE validity_test
--
ok
==
DROP TABLE string_test
--
ok
==
CREATE TABLE post_checkpoint_mem (a INT NOT NULL, b INT NOT NULL, c INT NOT NULL, d INT NOT NULL, e INT NOT NULL)
--
ok
==
INSERT INTO post_checkpoint_mem VALUES (1, 2, 3, 4, 5)
--
ok
==
INSERT INTO post_checkpoint_mem VALUES (6, 7, 8, 9, 10)
--
ok
==
SELECT CASE WHEN memory_usage_bytes < 2000
            THEN 'ok'
            ELSE error(concat('expected < 2000 bytes after checkpoint, got ', memory_usage_bytes))
       END
FROM duckdb_memory()
WHERE tag = 'IN_MEMORY_TABLE'
--
ok
==
DROP TABLE post_checkpoint_mem
--
ok
==
