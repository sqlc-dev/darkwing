# source: test/sql/window/test_scalar_window.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

SELECT row_number() OVER ()
--
ok
==
SELECT avg(42) OVER ()
--
ok
==
SELECT concat() OVER ()
--
ok
==
SELECT nonexistingfunction() OVER ()
--
ok
==
SELECT avg(row_number() over ()) over ()
--
ok
==
SELECT avg(42) over (partition by row_number() over ())
--
error: Parser Error: window functions are not allowed in window definitions
==
SELECT avg(42) over (order by row_number() over ())
--
error: Parser Error: window functions are not allowed in window definitions
==
