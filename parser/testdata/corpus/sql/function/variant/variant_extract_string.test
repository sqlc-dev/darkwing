# source: test/sql/function/variant/variant_extract_string.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

select variant_extract_string('{"a": 1, "b": {"c": 12, "d": 10}}');
--
ok
==
select variant_extract_string(a, '') from struct_cast_data();
--
ok
==
select variant_extract_string(a, []) from struct_cast_data();
--
ok
==
select variant_extract_string(a, ['']) from struct_cast_data();
--
ok
==
select variant_extract_string(a, 'c') from struct_cast_data();
--
ok
==
select variant_extract_string(a, ['c']) from struct_cast_data();
--
ok
==
select variant_extract_string(a, ['b', 'c']) from struct_cast_data();
--
ok
==
select variant_extract_string('{}'::VARIANT, '');
--
ok
==
select variant_extract_string(['duck', 'goose']::VARIANT, '');
--
ok
==
select variant_extract_string({'a': 1, 'b': ['duck', 'goose']}::VARIANT, 'b');
--
ok
==
select variant_extract_string(MAP {'a': 1, 'b': 2}::VARIANT, 'a');
--
ok
==
SELECT [*COLUMNS(*)] FROM (
	SELECT variant_extract_string(
		COLUMNS(lambda c: NOT regexp_matches(c, 'array|struct|map|union|tuple'))::VARIANT,
		''
	)
	FROM test_all_types()
	-- strip NULLs from output
	WHERE bool IS NOT NULL
);
--
ok
==
SELECT variant_extract_string(float::VARIANT, '')::DOUBLE = float::DOUBLE,
       variant_extract_string(double::VARIANT, '')::DOUBLE = double
FROM test_all_types()
WHERE bool IS NOT NULL;
--
ok
==
SELECT variant_extract_string({'uuid': uuid}::VARIANT, '')
FROM test_all_types()
WHERE bool = false;
--
ok
==
SELECT variant_extract_string({'blob': blob}::VARIANT, '')
FROM test_all_types()
WHERE bool = false;
--
ok
==
SELECT variant_extract_string({'bit': bit}::VARIANT, '')
FROM test_all_types()
WHERE bool = false;
--
ok
==
SELECT variant_extract_string({'interval': interval}::VARIANT, '')
FROM test_all_types()
WHERE bool = false;
--
ok
==
SELECT variant_extract_string({'timestamp': timestamp}::VARIANT, '')
FROM test_all_types()
WHERE bool = false;
--
ok
==
SELECT variant_extract_string({'timestamp_tz': timestamp_tz}::VARIANT, '')
FROM test_all_types()
WHERE bool = false;
--
ok
==
SELECT variant_extract_string({'geometry': geometry}::VARIANT, '')
FROM test_all_types()
WHERE bool = false;
--
ok
==
SELECT variant_extract_string(chr({val})::VARIANT, '') == chr({val})
--
error: Parser Error: syntax error at or near "}"
==
SELECT variant_extract_string(
	[chr(34), chr(92), chr(8), chr(12), chr(10), chr(13), chr(9), chr(1), '🦆']::VARIANT,
	''
);
--
ok
==
select variant_extract_string(NULL::VARIANT, '');
--
ok
==
select variant_extract_string(NULL, '');
--
ok
==
select variant_extract_string(NULL::VARIANT, 'a');
--
ok
==
select variant_extract_string(j, 'a') from (values ({'a': {'x': 1}}::VARIANT), (NULL::VARIANT)) t(j);
--
ok
==
select variant_extract_string(j, ['a', 'b']) from (values ({'a': {'x': 1}}::VARIANT), (NULL::VARIANT)) t(j);
--
ok
==
select variant_extract_string({'a': 1, 'b': null}::VARIANT, 'b');
--
ok
==
select variant_extract_string('{}'::VARIANT, 'a');
--
ok
==
select variant_extract_string({'a': 1, 'b': {'c': 12, 'd': 10}}::VARIANT, 'q');
--
ok
==
select variant_extract_string({'a': 1}::VARIANT, NULL);
--
ok
==
select variant_extract_string({'a': 1, 'b': {'c': {'d': 12}}}::VARIANT, '$.b.c');
--
ok
==
select variant_extract_string({'a': 1}::VARIANT);
--
ok
==
select variant_extract_string({'a': 1}::VARIANT, [1]);
--
ok
==
select variant_extract_string({'a': 1}::VARIANT, [NULL]);
--
ok
==
select variant_extract_string({'a': 1}::VARIANT, [NULL::VARCHAR]);
--
ok
==
select variant_extract_string({'c': 1}::VARIANT, ['c', NULL]::VARCHAR[]);
--
ok
==
select variant_extract_string({'c': 1}::VARIANT, []::VARCHAR[]);
--
ok
==
