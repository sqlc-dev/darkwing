# source: test/sql/copy/parquet/writer/row_group_size_bytes.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

copy (select 42) to '{TEST_DIR}/tbl.parquet' (ROW_GROUP_SIZE_BYTES)
--
ok
==
copy (
    select range c0,
           range c1,
           range c2,
           range c3,
           range c4,
           range c5,
           range c6,
           range c7,
    from range(50000)
) to '{TEST_DIR}/tbl.parquet' (ROW_GROUP_SIZE_BYTES '1mb')
--
ok
==
select max(row_group_num_rows) from parquet_metadata('{TEST_DIR}/tbl.parquet')
--
ok
==
select min(row_group_bytes) != 0 from parquet_metadata('{TEST_DIR}/tbl.parquet')
--
ok
==
copy (
    select range c0,
           range c1,
           range c2,
           range c3,
           range c4,
           range c5,
           range c6,
           range c7,
    from range(50000)
) to '{TEST_DIR}/tbl.parquet' (ROW_GROUP_SIZE_BYTES 500000)
--
ok
==
copy (
    select range c0,
           range c1,
           range c2,
           range c3,
           range c4,
           range c5,
           range c6,
           range c7,
    from range(50000)
) to '{TEST_DIR}/tbl.parquet' (ROW_GROUP_SIZE 10000, ROW_GROUP_SIZE_BYTES '1GB')
--
ok
==
copy (
    select range || repeat('0', 50) c0
    from range(50000)
) to '{TEST_DIR}/tbl.parquet' (ROW_GROUP_SIZE_BYTES 200000)
--
ok
==
copy (
    select range || repeat('0', 50) c0
    from range(50000)
) to '{TEST_DIR}/tbl.parquet' (ROW_GROUP_SIZE_BYTES 650000)
--
ok
==
