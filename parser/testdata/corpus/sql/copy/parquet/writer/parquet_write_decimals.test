# source: test/sql/copy/parquet/writer/parquet_write_decimals.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE decimals(
	dec4 DECIMAL(4,1),
	dec9 DECIMAL(9,2),
	dec18 DECIMAL(18,3),
	dec38 DECIMAL(38,4)
);
--
ok
==
INSERT INTO decimals VALUES (
	-999.9,
	-9999999.99,
	-999999999999999.999,
	-999999999999999999999999999999999.9999
), (
	NULL, NULL, NULL, NULL
), (
 	42, 42, 42, 42
), (
 	-42, -42, -42, -42
), (
  	0, 0, 0, 0
 ), (
  	999.9,
  	9999999.99,
  	999999999999999.999,
  	999999999999999999999999999999999.9999
);
--
ok
==
COPY decimals TO '{TEST_DIR}/decimals.parquet';
--
ok
==
SELECT * FROM decimals;
--
ok
==
SELECT * FROM '{TEST_DIR}/decimals.parquet';
--
ok
==
SELECT stats_min, stats_max, stats_min_value, stats_max_value FROM parquet_metadata('{TEST_DIR}/decimals.parquet');
--
ok
==
DELETE FROM decimals WHERE dec4<-42 OR dec4>42
--
ok
==
SELECT * FROM '{TEST_DIR}/decimals.parquet' WHERE {dec_column}=42
--
error: Parser Error: syntax error at or near "}"
==
SELECT * FROM '{TEST_DIR}/decimals.parquet' WHERE {dec_column}=-43
--
error: Parser Error: syntax error at or near "}"
==
SELECT * FROM '{TEST_DIR}/decimals.parquet' WHERE {dec_column}=43
--
error: Parser Error: syntax error at or near "}"
==
SELECT sdec4.min, sdec4.max, sdec9.min, sdec9.max, sdec18.min, sdec18.max, sdec38.min, sdec38.max FROM (SELECT stats(dec4) sdec4, stats(dec9) sdec9, stats(dec18) sdec18, stats(dec38) sdec38 FROM '{TEST_DIR}/decimals.parquet' LIMIT 1)
--
ok
==
