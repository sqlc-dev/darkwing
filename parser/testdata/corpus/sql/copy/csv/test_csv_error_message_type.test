# source: test/sql/copy/csv/test_csv_error_message_type.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE venue (
    venueid          SMALLINT        NOT NULL /*PRIMARY KEY*/
    , venuename      VARCHAR (100)
    , venuecity      VARCHAR (30)
    , venuestate     CHAR (2)
    , venueseats     INTEGER
);
--
ok
==
copy venue from '{DATA_DIR}/csv/venue_pipe.csv'
--
ok
==
SELECT * FROM read_csv('{DATA_DIR}/csv/venue_pipe.csv', types=['SMALLINT','VARCHAR','VARCHAR','VARCHAR','INTEGER']);
--
ok
==
SELECT * FROM read_csv('{DATA_DIR}/csv/venue_pipe.csv', types={'venueseats':'INTEGER'});
--
ok
==
SELECT * FROM read_csv('{DATA_DIR}/csv/venue_pipe_big.csv', sample_size = 1);
--
ok
==
CREATE TABLE venue_2 (
    venueid          SMALLINT        NOT NULL /*PRIMARY KEY*/
    , venuename      VARCHAR (100)
    , venuecity      VARCHAR (30)
    , venuestate     CHAR (2)
    , venueseats     VARCHAR
);
--
ok
==
INSERT INTO venue_2 SELECT * FROM read_csv('{DATA_DIR}/csv/venue_pipe_big.csv', sample_size = 1);
--
ok
==
SELECT COUNT(*) from venue_2
--
ok
==
DROP TABLE venue_2
--
ok
==
INSERT INTO venue_2 SELECT * FROM read_csv('{DATA_DIR}/csv/venue_pipe_big.csv', sample_size = 1, types={'venueseats': 'VARCHAR'});
--
ok
==
INSERT INTO venue_2 SELECT * FROM read_csv('{DATA_DIR}/csv/venue_pipe_big.csv', sample_size = -1);
--
ok
==
copy venue_2 from '{DATA_DIR}/csv/venue_pipe_big.csv'
--
ok
==
INSERT INTO venue SELECT * FROM read_csv('{DATA_DIR}/csv/venue_pipe.csv');
--
ok
==
