# source: test/sql/copy/csv/copy_to_overwrite.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

COPY (SELECT * FROM range(5) t(i)) TO '{TEST_DIR}/copy_to_overwrite.csv' (HEADER)
--
ok
==
SELECT * FROM '{TEST_DIR}/copy_to_overwrite.csv'
--
ok
==
COPY (SELECT * FROM range(5, 10) t(i)) TO '{TEST_DIR}/copy_to_overwrite.csv' (HEADER)
--
ok
==
COPY (SELECT * FROM range(5) t(i)) TO '{TEST_DIR}/copy_to_overwrite.csv.gz' (HEADER)
--
ok
==
SELECT * FROM '{TEST_DIR}/copy_to_overwrite.csv.gz'
--
ok
==
COPY (SELECT * FROM range(5, 10) t(i)) TO '{TEST_DIR}/copy_to_overwrite.csv.gz' (HEADER)
--
ok
==
SET preserve_insertion_order=false
--
ok
==
