# source: test/sql/attach/attach_no_wal_writes_mode.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

SET threads = 1;
--
ok
==
ATTACH '{TEST_DIR}/no_wal_writes.db' AS no_wal_writes (RECOVERY_MODE NO_WAL_WRITES);
--
ok
==
CREATE TABLE no_wal_writes.tbl AS SELECT range AS id, 0 AS v FROM range(5_000);
--
ok
==
UPDATE no_wal_writes.tbl SET v = id;
--
ok
==
DETACH no_wal_writes;
--
ok
==
SELECT COUNT(*) FROM glob('{TEST_DIR}/no_wal_writes.db.wal')
--
ok
==
SELECT COUNT(*) FROM no_wal_writes.tbl WHERE v = 0;
--
ok
==
ATTACH '{TEST_DIR}/other_no_wal_writes.db' AS no_wal_writes (RECOVERY_MODE NO_WAL_WRITES);
--
ok
==
CHECKPOINT no_wal_writes;
--
ok
==
SELECT COUNT(*) FROM glob('{TEST_DIR}/other_no_wal_writes.db.wal')
--
ok
==
