# source: test/sql/window/test_value_exclude.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

WITH src(id, v) AS (
	VALUES
	(1, 10),
	(2, 20)
)
SELECT
	id,
	{vfunc}(v ORDER BY v DESC) OVER (
	  ORDER BY id
	  ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING
	  EXCLUDE CURRENT ROW
	) AS actual
FROM src
ORDER BY id;
--
error: Parser Error: syntax error at or near "}"
==
WITH src(id, v) AS (
	VALUES
	(1, 10),
	(2, 20)
)
SELECT
	id,
	NTH_VALUE(v, 0 ORDER BY v DESC) OVER (
	  ORDER BY id
	  ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING
	  EXCLUDE CURRENT ROW
	) AS actual
FROM src
ORDER BY id;
--
ok
==
