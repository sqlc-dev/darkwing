# source: test/sql/copy/return_stats_truncate.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

COPY (SELECT repeat('\xFF', 300)::BLOB as blob) TO '{TEST_DIR}/test_truncate_blob.parquet' (RETURN_STATS);
--
ok
==
SELECT min_is_exact, max_is_exact FROM parquet_metadata('{TEST_DIR}/test_truncate_blob.parquet');
--
ok
==
COPY (SELECT ('\xFE' || repeat('\xFF', 300))::BLOB as blob) TO '{TEST_DIR}/test_truncate_blob.parquet' (RETURN_STATS);
--
ok
==
COPY (SELECT repeat('🦆', 300) as str) TO '{TEST_DIR}/test_truncate_string.parquet' (RETURN_STATS);
--
ok
==
SELECT min_is_exact, max_is_exact FROM parquet_metadata('{TEST_DIR}/test_truncate_string.parquet');
--
ok
==
COPY (SELECT 'B' || repeat('🦆', 300) as str) TO '{TEST_DIR}/test_truncate_string.parquet' (RETURN_STATS);
--
ok
==
