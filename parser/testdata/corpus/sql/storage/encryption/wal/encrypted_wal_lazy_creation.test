# source: test/sql/storage/encryption/wal/encrypted_wal_lazy_creation.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

ATTACH '{TEST_DIR}/attach_no_wal_{cipher}.db' AS attach_no_wal (ENCRYPTION_KEY 'asdf', ENCRYPTION_CIPHER '{cipher}');
--
ok
==
CREATE TABLE attach_no_wal.integers(i INTEGER);
--
ok
==
INSERT INTO attach_no_wal.integers FROM range(10000);
--
ok
==
DETACH attach_no_wal;
--
ok
==
SELECT COUNT(*) FROM glob('{TEST_DIR}/attach_no_wal_{cipher}.db.wal');
--
ok
==
SELECT COUNT(*) FROM attach_no_wal.integers;
--
ok
==
