# source: test/sql/copy/parquet/parquet_stats.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

select stats_min, stats_max, stats_min_value, stats_max_value from parquet_metadata('{DATA_DIR}/parquet-testing/boolean_stats.parquet');
--
ok
==
select stats_min, stats_max, stats_min_value, stats_max_value from parquet_metadata('{DATA_DIR}/parquet-testing/signed_stats.parquet');
--
ok
==
select * from '{DATA_DIR}/parquet-testing/signed_stats.parquet';
--
ok
==
select stats_min, stats_max, stats_min_value, stats_max_value from parquet_metadata('{DATA_DIR}/parquet-testing/unsigned_stats.parquet');
--
ok
==
select * from '{DATA_DIR}/parquet-testing/unsigned_stats.parquet';
--
ok
==
select stats_min, stats_max, stats_min_value, stats_max_value from parquet_metadata('{DATA_DIR}/parquet-testing/date_stats.parquet');
--
ok
==
select * from '{DATA_DIR}/parquet-testing/date_stats.parquet';
--
ok
==
select stats_min, stats_max, stats_min_value, stats_max_value from parquet_metadata('{DATA_DIR}/parquet-testing/varchar_stats.parquet');
--
ok
==
select min(str_val), max(str_val), min("hello\x00world"), max("hello\x00world") from '{DATA_DIR}/parquet-testing/varchar_stats.parquet';
--
ok
==
select * from '{DATA_DIR}/parquet-testing/varchar_stats.parquet';
--
ok
==
select stats_min, stats_max, stats_min_value, stats_max_value from parquet_metadata('{DATA_DIR}/parquet-testing/decimal_stats.parquet');
--
ok
==
select * from '{DATA_DIR}/parquet-testing/decimal_stats.parquet';
--
ok
==
select stats_min, stats_max, stats_min_value, stats_max_value from parquet_metadata('{DATA_DIR}/parquet-testing/arrow/int32_decimal.parquet');
--
ok
==
SELECT * FROM '{DATA_DIR}/parquet-testing/arrow/int32_decimal.parquet'
--
ok
==
select stats_min, stats_max, stats_min_value, stats_max_value from parquet_metadata('{DATA_DIR}/parquet-testing/arrow/int64_decimal.parquet');
--
ok
==
SELECT * FROM '{DATA_DIR}/parquet-testing/arrow/int64_decimal.parquet'
--
ok
==
SELECT stats_min, stats_max, stats_min_value, stats_max_value FROM parquet_metadata('{DATA_DIR}/parquet-testing/data-types.parquet')
--
ok
==
SELECT * FROM '{DATA_DIR}/parquet-testing/data-types.parquet'
--
ok
==
select * from parquet_metadata('{parquet_file}');
--
ok
==
copy (select '' i) to '{TEMP_DIR}/test.parquet';
--
ok
==
select i is null c0 from '{TEMP_DIR}/test.parquet';
--
ok
==
select stats_min_value is null c0, stats_max_value is null c1 from parquet_metadata('{TEMP_DIR}/test.parquet');
--
ok
==
select row_group_bytes, row_group_compressed_bytes from parquet_metadata('{TEMP_DIR}/test.parquet');
--
ok
==
select row_group_bytes, row_group_compressed_bytes from parquet_metadata('{DATA_DIR}/parquet-testing/varchar_stats.parquet');
--
ok
==
COPY (SELECT i::INTEGER AS i FROM range(8192) t(i)) TO '{TEST_DIR}/minmax_cast_stats.parquet' (FORMAT PARQUET, ROW_GROUP_SIZE 2048);
--
ok
==
SELECT min(i::BIGINT), max(i::BIGINT), typeof(min(i::BIGINT)), typeof(max(i::BIGINT))
FROM read_parquet('{TEST_DIR}/minmax_cast_stats.parquet');
--
ok
==
EXPLAIN ANALYZE SELECT min(i::BIGINT), max(i::BIGINT)
FROM read_parquet('{TEST_DIR}/minmax_cast_stats.parquet');
--
ok
==
SELECT min(TRY_CAST(i AS BIGINT)), max(TRY_CAST(i AS BIGINT)), typeof(min(TRY_CAST(i AS BIGINT))), typeof(max(TRY_CAST(i AS BIGINT)))
FROM read_parquet('{TEST_DIR}/minmax_cast_stats.parquet');
--
ok
==
EXPLAIN ANALYZE SELECT min(TRY_CAST(i AS BIGINT)), max(TRY_CAST(i AS BIGINT))
FROM read_parquet('{TEST_DIR}/minmax_cast_stats.parquet');
--
ok
==
