# source: test/sql/copy/partitioned/hive_partition_append.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE sensor_data(ts TIMESTAMP, value INT);
--
ok
==
INSERT INTO sensor_data VALUES
   (TIMESTAMP '2000-01-01 01:02:03', 42),
   (TIMESTAMP '2000-02-01 01:02:03', 100),
   (TIMESTAMP '2000-03-01 12:11:10', 1000)
;
--
ok
==
COPY (SELECT YEAR(ts) AS year, MONTH(ts) AS month, * FROM sensor_data)
TO '{TEST_DIR}/partitioned_append' (FORMAT PARQUET, PARTITION_BY (year, month), APPEND);
--
ok
==
SELECT year, month, SUM(value) FROM '{TEST_DIR}/partitioned_append/**/*.parquet' GROUP BY ALL ORDER BY ALL
--
ok
==
DELETE FROM sensor_data;
--
ok
==
INSERT INTO sensor_data VALUES
   (TIMESTAMP '2000-01-01 02:02:03', 62),
   (TIMESTAMP '2000-03-01 13:11:10', 50)
;
--
ok
==
COPY (SELECT YEAR(ts) AS year, MONTH(ts) AS month, * FROM sensor_data)
TO '{TEST_DIR}/partitioned_append' (FORMAT PARQUET, PARTITION_BY (year, month), APPEND, FILENAME_PATTERN 'my_pattern_{uuid}');
--
ok
==
COPY (SELECT YEAR(ts) AS year, MONTH(ts) AS month, * FROM sensor_data)
TO '{TEST_DIR}/partitioned_append' (FORMAT PARQUET, PARTITION_BY (year, month), FILENAME_PATTERN 'my_pattern_{uuid}', APPEND);
--
ok
==
COPY (SELECT YEAR(ts) AS year, MONTH(ts) AS month, * FROM sensor_data)
TO '{TEST_DIR}/partitioned_append' (FORMAT PARQUET, PARTITION_BY (year, month), APPEND, FILENAME_PATTERN 'my_pattern_without_uuid');
--
ok
==
