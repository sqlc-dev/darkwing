# source: test/fuzzer/sqlsmith/window-rows-overflow.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

create table all_types as
	select * exclude(
		small_enum,
		medium_enum,
		large_enum,
	)
from test_all_types();
--
ok
==
SELECT nth_value(1929, 42) OVER (
	PARTITION BY (
		"usmallint" > "smallint"
	), 747 ROWS BETWEEN COALESCE("bigint", 0) PRECEDING AND CURRENT ROW
)
FROM all_types;
--
ok
==
SELECT nth_value(1929, 42) OVER (
	PARTITION BY (
		"usmallint" > "smallint"
	), 747 ROWS BETWEEN 1 PRECEDING AND COALESCE("bigint", 0) PRECEDING
)
FROM all_types;
--
ok
==
SELECT nth_value(1929, 42) OVER (
	PARTITION BY (
		"usmallint" > "smallint"
	), 747 ROWS BETWEEN COALESCE("bigint", 0) FOLLOWING AND 1 FOLLOWING
)
FROM all_types;
--
ok
==
SELECT nth_value(1929, 42) OVER (
	PARTITION BY (
		"usmallint" > "smallint"
	), 747 ROWS BETWEEN CURRENT ROW AND COALESCE("bigint", 0) FOLLOWING
)
FROM all_types;
--
ok
==
