# source: test/sql/copy/csv/rejects/test_multiple_errors_same_line.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/cast_and_more_col.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, strict_mode=True);
--
ok
==
SElECT * EXCLUDE (scan_id) FROM reject_errors ORDER BY ALL;
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/multiple_cast_implicit.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, strict_mode=True);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/multiple_casts_flush.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'tomorrow': 'DATE'},
    store_rejects = true, auto_detect=false, header = 1);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/multiple_casts_mixed.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/cast_and_less_col.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/cast_and_maxline.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/less_col_and_max_line.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40);
--
ok
==
SElECT * EXCLUDE (scan_id) FROM reject_errors ORDER BY byte_position;
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/more_col_and_max_line.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40, strict_mode=True);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/unquoted_cast.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40, strict_mode=true);
--
ok
==
SElECT * EXCLUDE (scan_id)  FROM reject_errors ORDER BY byte_position;
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/unquoted_less.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40, strict_mode=true);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/unquoted_maxline.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40, strict_mode=true);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/unquoted_more.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40, strict_mode=true);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/invalid_utf_cast.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/invalid_utf_less.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/invalid_utf_max_line.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40, strict_mode=True);
--
ok
==
SElECT * EXCLUDE (scan_id)  FROM reject_errors ORDER BY byte_position, error_message;
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/invalid_utf_more.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40, strict_mode=True);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/invalid_utf_unquoted.csv',
    columns = {'name': 'VARCHAR', 'last_name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40, strict_mode=true);
--
ok
==
FROM read_csv('{DATA_DIR}/csv/rejects/multiple_errors/multiple_errors.csv',
    columns = {'name': 'VARCHAR', 'age': 'INTEGER', 'current_day': 'DATE', 'barks': 'INTEGER'},
    store_rejects = true, auto_detect=false, header = 1, max_line_size=40, strict_mode=true);
--
ok
==
