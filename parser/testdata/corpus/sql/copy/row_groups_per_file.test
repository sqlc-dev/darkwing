# source: test/sql/copy/row_groups_per_file.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

COPY bigdata TO '{TEST_DIR}/row_groups_per_file1' (FORMAT PARQUET, ROW_GROUP_SIZE 2000, ROW_GROUPS_PER_FILE 1)
--
ok
==
SELECT count(*) FROM '{TEST_DIR}/row_groups_per_file1/*.parquet'
--
ok
==
SELECT count(*) FROM glob('{TEST_DIR}/row_groups_per_file1/*.parquet')
--
ok
==
COPY bigdata TO '{TEST_DIR}/row_groups_per_file2' (FORMAT PARQUET, ROW_GROUP_SIZE 4000, ROW_GROUPS_PER_FILE 1)
--
ok
==
SELECT count(*) FROM '{TEST_DIR}/row_groups_per_file2/*.parquet'
--
ok
==
SELECT count(*) FROM glob('{TEST_DIR}/row_groups_per_file2/*.parquet')
--
ok
==
COPY bigdata TO '{TEST_DIR}/row_groups_per_file3' (FORMAT PARQUET, ROW_GROUP_SIZE 2000, ROW_GROUPS_PER_FILE 2)
--
ok
==
SELECT count(*) FROM '{TEST_DIR}/row_groups_per_file3/*.parquet'
--
ok
==
SELECT count(*) FROM glob('{TEST_DIR}/row_groups_per_file3/*.parquet')
--
ok
==
COPY bigdata TO '{TEST_DIR}/row_groups_per_file_stats/' (FORMAT PARQUET, WRITE_EMPTY_FILE false, FILENAME_PATTERN '{uuid}', ROW_GROUP_SIZE 3000, ROW_GROUPS_PER_FILE 2, RETURN_STATS);
--
ok
==
COPY bigdata TO '{TEST_DIR}/row_groups_per_file4' (FORMAT PARQUET, ROW_GROUP_SIZE 2000, ROW_GROUPS_PER_FILE 1)
--
ok
==
SELECT count(*) FROM '{TEST_DIR}/row_groups_per_file4/*.parquet'
--
ok
==
SELECT count(*) BETWEEN 1 and 10 FROM glob('{TEST_DIR}/row_groups_per_file4/*.parquet')
--
ok
==
COPY bigdata TO '{TEST_DIR}/row_groups_per_file5' (FORMAT PARQUET, ROW_GROUP_SIZE 4000, ROW_GROUPS_PER_FILE 1)
--
ok
==
SELECT count(*) FROM '{TEST_DIR}/row_groups_per_file5/*.parquet'
--
ok
==
SELECT count(*) BETWEEN 1 and 10 FROM glob('{TEST_DIR}/row_groups_per_file5/*.parquet')
--
ok
==
COPY bigdata TO '{TEST_DIR}/row_groups_per_file6' (FORMAT PARQUET, ROW_GROUP_SIZE 2000, ROW_GROUPS_PER_FILE 2)
--
ok
==
SELECT count(*) FROM '{TEST_DIR}/row_groups_per_file6/*.parquet'
--
ok
==
SELECT count(*) BETWEEN 1 and 10 FROM glob('{TEST_DIR}/row_groups_per_file6/*.parquet')
--
ok
==
COPY bigdata TO '{TEST_DIR}/row_groups_per_file7' (FORMAT PARQUET, PER_THREAD_OUTPUT TRUE, ROW_GROUP_SIZE 2000, ROW_GROUPS_PER_FILE 1)
--
ok
==
SELECT count(*) FROM '{TEST_DIR}/row_groups_per_file7/*.parquet'
--
ok
==
SELECT count(*) BETWEEN 1 AND 10 FROM glob('{TEST_DIR}/row_groups_per_file7/*.parquet')
--
ok
==
COPY bigdata TO '{TEST_DIR}/row_groups_per_file8' (FORMAT PARQUET, PER_THREAD_OUTPUT TRUE, ROW_GROUP_SIZE 4000, ROW_GROUPS_PER_FILE 1)
--
ok
==
SELECT count(*) FROM '{TEST_DIR}/row_groups_per_file8/*.parquet'
--
ok
==
SELECT count(*) BETWEEN 1 AND 10 FROM glob('{TEST_DIR}/row_groups_per_file8/*.parquet')
--
ok
==
COPY bigdata TO '{TEST_DIR}/row_groups_per_file9' (FORMAT PARQUET, PER_THREAD_OUTPUT TRUE, ROW_GROUP_SIZE 2000, ROW_GROUPS_PER_FILE 2)
--
ok
==
SELECT count(*) FROM '{TEST_DIR}/row_groups_per_file9/*.parquet'
--
ok
==
SELECT count(*) BETWEEN 1 AND 10 FROM glob('{TEST_DIR}/row_groups_per_file9/*.parquet')
--
ok
==
COPY bigdata TO '{TEST_DIR}/row_groups_per_file_error' (FORMAT PARQUET, ROW_GROUPS_PER_FILE 1, USE_TMP_FILE TRUE);
--
ok
==
CREATE TABLE partitioned_bigdata AS
SELECT col_a % 4 AS part, col_a, col_b FROM bigdata;
--
ok
==
COPY partitioned_bigdata
TO '{TEST_DIR}/row_groups_per_file_partitioned'
(FORMAT PARQUET, ROW_GROUP_SIZE 1000, ROW_GROUPS_PER_FILE 1, PARTITION_BY part);
--
ok
==
SELECT count(*)
FROM read_parquet('{TEST_DIR}/row_groups_per_file_partitioned/**/*.parquet', hive_partitioning = true);
--
ok
==
WITH files AS (
	SELECT part, count(DISTINCT filename) file_count
	FROM read_parquet(
		'{TEST_DIR}/row_groups_per_file_partitioned/**/*.parquet',
		filename = true,
		hive_partitioning = true
	)
	GROUP BY part
)
SELECT min(file_count) > 1
FROM files;
--
ok
==
