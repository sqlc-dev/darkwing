# source: test/sql/types/struct/struct_projection_pushdown_optimizer_bug.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE tbl(
	-- 0
	s1 STRUCT(
		i SMALLINT
	),
	-- 1
	s2 STRUCT(
		f DATE
	),
	-- 2
	id INT
);
--
ok
==
INSERT INTO tbl VALUES
	(ROW(1), ROW(DATE '2024-01-30'), 0);
--
ok
==
WITH subq AS
  (FROM tbl) -- project 0,1,2
SELECT 
       id, -- 2
       min(subq.s1.i), -- 0.0
       min(subq.s2.f) -- 1.0
FROM subq
GROUP BY id;
--
ok
==
