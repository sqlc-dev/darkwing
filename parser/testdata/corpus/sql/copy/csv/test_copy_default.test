# source: test/sql/copy/csv/test_copy_default.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

CREATE TABLE test (a INTEGER, b VARCHAR DEFAULT('hello'), c INTEGER DEFAULT(3+4));
--
ok
==
COPY test (a) FROM '{DATA_DIR}/csv/test/test_default.csv';
--
ok
==
COPY test (c) FROM '{DATA_DIR}/csv/test/test_default.csv';
--
ok
==
SELECT COUNT(a), COUNT(b), COUNT(c), MIN(LENGTH(b)), MAX(LENGTH(b)), SUM(a), SUM(c) FROM test;
--
ok
==
