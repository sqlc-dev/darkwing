# source: test/sql/copy/csv/null_terminator.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

FROM '{DATA_DIR}/csv/null_terminator.csv'
--
ok
==
FROM read_csv('{DATA_DIR}/csv/null_terminator.csv', header = 0)
--
ok
==
FROM read_csv('{DATA_DIR}/csv/null_terminator.csv', header = 0, escape = '')
--
ok
==
FROM read_csv('{DATA_DIR}/csv/null_terminator.csv', header = 0, delim = '\t', quote = '', escape = '')
--
ok
==
create or replace table t as (from values ('a' || chr(0) || 'b') t(i));
--
ok
==
copy t to '{TEMP_DIR}/csv2tsv.tsv' (header false, delimiter '\t', escape '', quote '');
--
ok
==
FROM read_csv('{TEMP_DIR}/csv2tsv.tsv', header = 0)
--
ok
==
