# source: test/sql/copy/csv/test_csv_mixed_casts.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

FROM read_csv(
    '{DATA_DIR}/csv/mixed_dates.csv',
    auto_detect = false,
    header = true,
     columns = {
      'A': 'INTEGER',
      'B': 'DATE',
    },
    dateformat = '%m/%d/%Y',
    ignore_errors = true
);
--
ok
==
FROM read_csv(
    '{DATA_DIR}/csv/mixed_timestamps.csv',
    auto_detect = false,
    header = true,
     columns = {
      'A': 'INTEGER',
      'B': 'TIMESTAMP',
    },
    timestampformat = '%m/%d/%Y %H:%M:%S',
    ignore_errors = true
);
--
ok
==
FROM read_csv(
    '{DATA_DIR}/csv/mixed_double.csv',
    auto_detect = false,
    header = true,
     columns = {
      'A': 'INTEGER',
      'B': 'FLOAT',
    },
    ignore_errors = true
);
--
ok
==
FROM read_csv(
    '{DATA_DIR}/csv/mixed_double.csv',
    auto_detect = false,
    header = true,
     columns = {
      'A': 'INTEGER',
      'B': 'DOUBLE',
    },
    ignore_errors = true
);
--
ok
==
FROM read_csv(
    '{DATA_DIR}/csv/mixed_double.csv',
    auto_detect = false,
    header = true,
     columns = {
      'A': 'INTEGER',
      'B': 'DECIMAL',
    },
    ignore_errors = true
);
--
ok
==
FROM read_csv(
    '{DATA_DIR}/csv/mixed_decimal.csv',
    auto_detect = false,
    header = true,
    delim = ';',
     columns = {
      'A': 'INTEGER',
      'B': 'DECIMAL',
    },
    ignore_errors = true
);
--
ok
==
FROM read_csv(
    '{DATA_DIR}/csv/mixed_decimal.csv',
    auto_detect = false,
    header = true,
    delim = ';',
    decimal_separator = ',',
     columns = {
      'A': 'INTEGER',
      'B': 'DECIMAL',
    },
    ignore_errors = true
);
--
ok
==
FROM read_csv(
    '{DATA_DIR}/csv/mixed_decimal.csv',
    auto_detect = false,
    header = true,
    delim = '|',
    decimal_separator = ';',
     columns = {
      'A': 'INTEGER',
      'B': 'DECIMAL',
    },
    ignore_errors = true
);
--
ok
==
