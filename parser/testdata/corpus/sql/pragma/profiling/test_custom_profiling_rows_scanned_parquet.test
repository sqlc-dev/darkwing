# source: test/sql/pragma/profiling/test_custom_profiling_rows_scanned_parquet.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

COPY (SELECT * FROM range(0, 10) as r(i)) TO '{TEST_DIR}/range.parquet' (FORMAT 'parquet');
--
ok
==
COPY (SELECT * FROM range(0, 10) as r(i)) TO '{TEST_DIR}/range2.parquet' (FORMAT 'parquet');
--
ok
==
SELECT * FROM '{TEST_DIR}/range.parquet' order by all;
--
ok
==
SELECT
	CASE WHEN query.total_rows_scanned = 10 THEN 'true'
	ELSE 'false' END
FROM metrics_output;
--
ok
==
