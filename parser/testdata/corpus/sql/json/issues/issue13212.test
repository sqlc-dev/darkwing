# source: test/sql/json/issues/issue13212.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

copy (select range::int64 as id, range::varchar as name from range(0)) to '{TEST_DIR}/t1.json.gz' (format json, compression gzip)
--
ok
==
copy (select id, name from values (1, 'bob'), (2, 'tom') tbl(id, name)) to '{TEST_DIR}/t2.json.gz' (format json, compression gzip)
--
ok
==
SELECT * FROM read_ndjson_auto(['{TEST_DIR}/t1.json.gz', '{TEST_DIR}/t2.json.gz']);
--
ok
==
SELECT * FROM read_ndjson(['{TEST_DIR}/t1.json.gz', '{TEST_DIR}/t2.json.gz'], columns={id: 'int64', name: 'varchar'});
--
ok
==
