# source: test/sql/window/test_window_finalize_race.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

PRAGMA threads=2
--
ok
==
WITH t AS (SELECT 0 AS x),
u AS (
    SELECT * FROM t WHERE x >= 5000
    UNION ALL
    SELECT * FROM t
)
SELECT x, count() OVER () FROM u ORDER BY x DESC;
--
ok
==
WITH final_cols AS (
    SELECT i AS x
    FROM range(1) t(i)
),
final_people AS (
    SELECT *
    FROM final_cols
    WHERE x >= 5000
    UNION ALL
    SELECT *
    FROM final_cols
    WHERE NOT EXISTS (
        SELECT 1
        FROM final_cols
        WHERE x >= 5000
    )
)
SELECT x, count() OVER () AS full_count
FROM final_people
ORDER BY x DESC
LIMIT 5;
--
ok
==
