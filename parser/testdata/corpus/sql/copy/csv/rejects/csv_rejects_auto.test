# source: test/sql/copy/csv/rejects/csv_rejects_auto.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

SELECT typeof(first(column0)), typeof(first(column1)), COUNT(*), SUM(column0), MAX(len(column1)) FROM read_csv_auto(
    '{DATA_DIR}/csv/error/mismatch/big_bad*.csv',
    sample_size=1,
    store_rejects=true);
--
ok
==
SELECT SUM(num) FROM read_csv_auto(
	'{DATA_DIR}/csv/error/mismatch/half1.csv',
	header=true,
	sample_size=1,
	store_rejects=true)
--
ok
==
SELECT COUNT(*) FROM reject_errors;
--
ok
==
SELECT SUM(num) FROM read_csv_auto(
	'{DATA_DIR}/csv/error/mismatch/half2.csv',
	header=true,
	ignore_errors=true,
	sample_size=1,
	rejects_table='csv_rejects_table');
--
ok
==
SELECT COUNT(*) FROM csv_rejects_table;
--
ok
==
DROP TABLE csv_rejects_table;
--
ok
==
