# source: test/sql/copy/parquet/alltypes-dictionaries.test
# pin: 8cbdaba6acb50db7780d22a65dd131584d472262
# oracle: v2.0.0-alpha38195
# generated by cmd/regenerate - do not edit

copy (select (r1.range * 10)::{type} r from range(10) r1, range(1000) r2) to '{TEST_DIR}/dict-{type}.parquet' (row_group_size 2048);
--
error: Parser Error: syntax error at or near "{"
==
select first(encodings) from parquet_metadata('{TEST_DIR}/dict-{type}.parquet') group by encodings;
--
ok
==
SELECT COUNT(*) from '{TEST_DIR}/dict-{type}.parquet' WHERE r='20'
--
ok
==
select column_id, BOOL_AND(bloom_filter_offset > 4), BOOL_AND(bloom_filter_length > 1)  from parquet_metadata('{TEST_DIR}/dict-{type}.parquet') group by column_id order by column_id;
--
ok
==
copy (select (r1.range * 10)::{type} r from range(10) r1, range(1000) r2) to '{TEST_DIR}/dict-{type}-v1.parquet' (row_group_size 2048, parquet_version v1);
--
error: Parser Error: syntax error at or near "{"
==
select first(encodings) from parquet_metadata('{TEST_DIR}/dict-{type}-v1.parquet') group by encodings;
--
ok
==
SELECT COUNT(*) from '{TEST_DIR}/dict-{type}-v1.parquet' WHERE r='20'
--
ok
==
select column_id, BOOL_AND(bloom_filter_offset > 4), BOOL_AND(bloom_filter_length > 1)  from parquet_metadata('{TEST_DIR}/dict-{type}-v1.parquet') group by column_id order by column_id;
--
ok
==
copy (select (r1.range * 10)::{type} r from range(10) r1, range(1000) r2) to '{TEST_DIR}/dict-{type}-v2.parquet' (row_group_size 2048, parquet_version v2);
--
error: Parser Error: syntax error at or near "{"
==
select first(encodings) from parquet_metadata('{TEST_DIR}/dict-{type}-v2.parquet') group by encodings;
--
ok
==
SELECT COUNT(*) from '{TEST_DIR}/dict-{type}-v2.parquet' WHERE r='20'
--
ok
==
select column_id, BOOL_AND(bloom_filter_offset > 4), BOOL_AND(bloom_filter_length > 1)  from parquet_metadata('{TEST_DIR}/dict-{type}-v2.parquet') group by column_id order by column_id;
--
ok
==
